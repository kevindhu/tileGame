var WIDTH = 3000;
var HEIGHT = 3000;
var TILES = 1000;
var SHARDS = 20;
var SHARD_WIDTH = 10;
var BORDER_WIDTH = 100;

module.exports = {
    WIDTH: WIDTH,
    HEIGHT: HEIGHT,
    TILES: TILES,
    SHARDS: SHARDS,
    SHARD_WIDTH: SHARD_WIDTH,
    BORDER_WIDTH: BORDER_WIDTH
};