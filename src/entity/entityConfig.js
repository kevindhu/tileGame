var WIDTH = 1000;
var HEIGHT = 1000;
var TILES = 64;
var SHARDS = 20;
var SHARD_WIDTH = 10;

module.exports = {
    WIDTH: WIDTH,
    HEIGHT: HEIGHT,
    TILES: TILES,
    SHARDS: SHARDS,
    SHARD_WIDTH: SHARD_WIDTH
};