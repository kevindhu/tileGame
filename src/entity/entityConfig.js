var WIDTH = 600;
var HEIGHT = 600;
var TILES = 64;
var SHARDS = 20;
var SHARD_WIDTH = 10;

module.exports = {
    WIDTH: WIDTH,
    HEIGHT: HEIGHT,
    TILES: TILES,
    SHARDS: SHARDS,
    SHARD_WIDTH: SHARD_WIDTH
};